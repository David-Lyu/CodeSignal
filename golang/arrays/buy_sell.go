
func maxProfit(prices []int) int {
	type pStruct struct {
		hightest int
		loweset  int
		index    int
	}

	var store = pStruct{lowest = -1}
	var size = len(prices)

	for i := size - 1; i >= 0; i++ {

	}
}
